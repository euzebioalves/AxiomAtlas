@model Axiom.Atlas.Web.Model.Login.ForgotPasswordViewModel
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="pt-br">
<head>
    <title>Esqueci a Senha - Axiom Atlas</title>
    <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="shortcut icon" href="~/favicon.ico" />

    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Inter:300,400,500,600,700" />
    <link href="~/metronic8/css/plugins.bundle.css" rel="stylesheet" type="text/css" />
    <link href="~/metronic8/css/style.bundle.css" rel="stylesheet" type="text/css" />
    <script>
            if (window.top != window.self) {
                window.top.location.replace(window.self.location.href);
            }
    </script>
</head>
<body id="kt_body" class="app-blank bgi-size-cover bgi-attachment-fixed bgi-position-center">
    <script>
            var defaultThemeMode = "light";
            var themeMode;
            if ( document.documentElement ) {
                if ( document.documentElement.hasAttribute("data-bs-theme-mode")) {
                    themeMode = document.documentElement.getAttribute("data-bs-theme-mode");
                } else {
                    if ( localStorage.getItem("data-bs-theme") !== null ) {
                        themeMode = localStorage.getItem("data-bs-theme");
                    } else {
                        themeMode = defaultThemeMode;
                    }
                }
                if (themeMode === "system") {
                    themeMode = window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light";
                }
                document.documentElement.setAttribute("data-bs-theme", themeMode);
            }
    </script>
    <div class="d-flex flex-column flex-root" id="kt_app_root">
        <style>
                    body {
                        background-image: url('~/metronic8/images/bg10.jpeg');
                    }

                    [data-bs-theme="dark"] body {
                        background-image: url('~/metronic8/images/bg10-dark.jpeg');
                    }
        </style>
        <div class="d-flex flex-column flex-lg-row flex-column-fluid">
            <div class="d-flex flex-lg-row-fluid">
                <div class="d-flex flex-column flex-center pb-0 pb-lg-10 p-10 w-100">
                    <img class="theme-light-show mx-auto mw-100 w-150px w-lg-300px mb-10 mb-lg-20" src="~/metronic8/images/agency.png" alt="" />
                    <img class="theme-dark-show mx-auto mw-100 w-150px w-lg-300px mb-10 mb-lg-20" src="~/metronic8/images/agency-dark.png" alt="" />
                    <h1 class="text-gray-800 fs-2qx fw-bold text-center mb-7">
                        Recuperação segura de acesso
                    </h1>
                    <div class="text-gray-600 fs-base text-center fw-semibold">
                        Informe seu e-mail corporativo para iniciar o fluxo de redefinição de senha do Axiom Atlas.
                    </div>
                </div>
            </div>
            <div class="d-flex flex-column-fluid flex-lg-row-auto justify-content-center justify-content-lg-end p-12">
                <div class="bg-body d-flex flex-column flex-center rounded-4 w-md-600px p-10">
                    <div class="d-flex flex-center flex-column align-items-stretch h-lg-100 w-md-400px">
                        <div class="d-flex flex-center flex-column flex-column-fluid pb-15 pb-lg-20">
                            
                            <form class="form w-100" novalidate="novalidate" id="axiom_forgot_password_form" method="post" asp-action="ForgotPassword" asp-controller="Auth">
                                <div class="text-center mb-11">
                                    <div class="d-block d-lg-flex p-14 mb-8">
                                        <img alt="Logo" src="~/metronic8/fonts/default-dark.svg" class="logo-default">
                                    </div>
                                    <h1 class="text-gray-900 fw-bolder mb-3">
                                        Esqueceu sua senha?
                                    </h1>
                                    <div class="text-gray-500 fw-semibold fs-6">
                                        Informe seu e-mail para receber um link de redefinição.
                                    </div>
                                </div>

                                @if (!ViewData.ModelState.IsValid)
                                {
                                    <div class="alert alert-danger mb-8">
                                        @Html.ValidationSummary(true, "")
                                    </div>
                                }
                                <div class="fv-row mb-8">
                                    <input asp-for="Email" type="email" placeholder="E-mail" autocomplete="off" class="form-control form-control-solid">
                                </div>
                                <div class="d-flex flex-wrap justify-content-center pb-lg-0">
                                    <button type="submit" id="btn_submit_forgot" class="btn btn-primary me-4">
                                        <span class="indicator-label">Enviar Link</span>
                                        <span class="indicator-progress">
                                            Aguarde... <span class="spinner-border spinner-border-sm align-middle ms-2"></span>
                                        </span>
                                    </button>
                                    <a asp-controller="Auth" asp-action="Login" class="btn btn-light">Cancelar</a>
                                </div>
                            </form>
                        </div>
                        <div class="d-flex flex-center">
                            <span class="text-muted fw-semibold fs-7">Axiom Atlas © @DateTime.Now.Year</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="~/metronic8/js/plugins.bundle.js"></script>
    <script src="~/metronic8/js/scripts.bundle.js"></script>
    <script src="~/metronic8/js/general.js"></script>
    
    <script>
            document.getElementById('axiom_forgot_password_form').addEventListener('submit', function() {
                var btn = document.getElementById('btn_submit_forgot');
                btn.querySelector('.indicator-label').style.display = 'none';
                btn.querySelector('.indicator-progress').style.display = 'inline-block';
                btn.setAttribute('data-kt-indicator', 'on');
                // Removemos o disabled=true aqui para não bloquear o envio do POST padrão do MVC
            });
    </script>
</body>
</html>
